# Flat node_modules (no symlinks). pnpm's default isolated store uses symlinked
# directories, which Vercel cannot package into Serverless Functions ("invalid
# deployment package... files in symlinked directories"). Hoisted layout fixes it
# and also makes next.config outputFileTracingIncludes resolve real paths.
node-linker=hoisted
